package Game.GameEngine.User;

/**
 * Represents a remote player connected via server.
 * TODO: Implement networking for online multiplayer.
 */
public class ServerUser extends User {

    public ServerUser(String userName) {
        super(userName);
    }
}
